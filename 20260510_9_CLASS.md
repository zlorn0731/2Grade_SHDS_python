# 📚 Python

## 9주차 - 연립일차방정식과 행렬

### 연립일차방정식과 첨가행렬
- 연립일차방정식이란?
  - 두 개의 방정식을 동시에 만족하는 x, y를 찾으라는 뜻
  - 여러 개의 1차 방정식을 동시에 만족하는 값을 찾는 문제
  ```
  2x + 3y - 1 = 0
  x² + y² = 1
  ```
- 해(solution) : 방정식을 참(맞는 식)으로 만들어주는 값
- 해집합(solution set) : 모든 해들의 모임
- 방정식을 푼다(solving equation) : 해집합을 구하는 것
- 일차방정식(linear equation) : 미지수 차가 1인 방정식
```
a₁x₁ + a₂x₂ + ..... = b
```
- 동차방정식(homogeneous) : 상수항이 0인 방정식
  - 모든 항이 같은 종류(차수)이고, 오른쪽이 0인 방정식
```
a₁x₁ + a₂x₂ + ..... = 0
```
```
예시: 

일차방정식 X
x2+y=3

일차방정식 O, 동차방정식 O
2x+3y=0
```
- 해 : 연립일차방정식에서 각 방정식이 모두 성립하는 순서쌍
- 해집합 : 연립일차방정식 해 전체의 집합
- 동치 : 동일한 해집합을 갖는 두 연립일차방정식
```
예시:

x + y = 4
x - y = 2

2x + 2y = 8
3x - 3y = 6

두 연립일차방정식의 해는 모두 (3, 1)이므로 동치
```

### 파이썬 코드
```
x + y = 4      2x + 2y = 8
x - y = 2      3x - 3y = 6
```
```
import numpy as np

# 계수 행렬 A
A = np.array([[1, 1],
              [1, -1]])
# 결과 벡터 b
b = np.array([4, 2])

# 해 구하기
solution = np.linalg.solve(A, b) # Ax = b 형태의 연립일차방정식을 자동으로 풀어 x를 구하는 함수
print("해: ", solution)
print("x: ", solution[0])
print("y: ", solution[1])

- 결과
해:  [3. 1.]
x:  3.0
y:  1.0
```

### 시각화 작업
```
import numpy as np
import matplotlib.pyplot as plt

# 마이너스 기호 깨짐 방지
plt.rcParams['axes.unicode_minus'] = False

# x값 범위
x = np.linspace(-10, 10, 100)

# 직선 식
y1 = 4 - x
y2 = x - 2

# 선 그래프
plt.plot(x, y1, label = 'x + y = 4')
plt.plot(x, y2, label = 'x - y = 2')

# 그래프 설정
plt.xlabel('x')
plt.ylabel('y')
plt.rc('font', family = 'Malgun Gothic')
plt.title('연립방정식 선그래프')
plt.legend()
plt.grid()

# 출력
plt.show()
```

### 파이썬 코드
```
import numpy as np

# 계수 행렬 A
A2 = np.array([[2, 2],
               [3, -3]])
# 결과 벡터 b
b2 = np.array([8, 6])

# 해 구하기
solution2 = np.linalg.solve(A2, b2)
print("해: ", solution2)

- 결과
해:  [3. 1.]
```

### 시각화 작업
```
import numpy as np
import matplotlib.pyplot as plt

# 마이너스 깨짐 방지
plt.rcParams['axes.unicode_minus'] = False

# x 범위
x = np.linspace(-10, 10, 100)

# 직선 식
# 2x + 2y = 8 -> y = 4 - x
y1 = 4 - x
# 3x - 3y = 6 -> y = x - 2
y2 = x - 2

# 선 그래프
plt.plot(x, y1, label = '2x + 2y = 8')
plt.plot(x, y2, label = '3x - 3y = 6')

# 그래프 설정
plt.xlabel('x')
plt.ylabel('y')
plt.title('연립방정식 선그래프')
plt.legend()
plt.grid()

# 출력
plt.show()
```

### 연립일차방정식과 첨가행렬
- 연립일차방정식의 해집합
```
연립일차방정식
            ↳ 해를 갖지 않는 경우
            ↳ 해를 갖는 경우
                         ↳ 유일한 해
                         ↳ 무수히 많은 해
```
- 해를 갖지 않는 경우
```
3x + 2y = 12
3x + 2y = 6
```
- 해를 갖는 경우
```
[1] 유일한 해를 갖는 경우

x - y = -1
3x + y = 9
```
```
[2] 무수히 많은 해를 갖는 경우

x + y = 2
3x + 3y = 6
```

### 파이썬 코드(오류)
```
import numpy as np

A = np.array([[3, 2],
              [3, 2]])
b = np.array([12, 6])

x = np.linalg.solve(A, b)
print("해: ", x)
```

### 파이썬 코드(오류 수정)
```
import numpy as np

A = np.array([[1, 1],
              [1, 1]])
b = np.array([2, 3])

try: # 코드 실행 중 오류가 나도 프로그램이 멈추지 않고 처리하도록 해주는 구조
    x = np.linalg.solve(A, b)
    print("해: ", x)
except np.linalg.LinAlgError:
    print("해가 없거나 무수히 많습니다.")
```

### 시각화 작업
```
import numpy as np
import matplotlib.pyplot as plt

# x 값 범위 설정
x = np.linspace(-10, 10, 100)
# 식 변형
# 3x + 2y = 12 -> y = (12 - 3x) / 2
y1 = (12 - 3 * x) / 2
# 3x + 2y = 6 -> y = (6 - 3x) / 2
y2 = (6 - 3 * x) / 2

# 그래프 그리기
plt.figure()
plt.plot(x, y1, label = '3x + 2y = 12')
plt.plot(x, y2, label = '3x + 2y = 6')

# 축과 제목
plt.xlabel('x')
plt.ylabel('y')
plt.title('연립방정식 그래프 (해 없음)')

# 범례 표시
plt.legend()
# 격자
plt.grid()
# 출력
plt.show()
```

### 파이썬 코드(유일한 해를 갖는 경우)
```
import numpy as np

A = np.array([[1, -1],
              [3, 1]])
b = np.array([-1, 9])

solution = np.linalg.solve(A, b)

print("해: ", solution)
print("x= ", solution[0])
print("y= ", solution[1])

- 결과
해:  [2. 3.]
x=  2.0
y=  3.0
```

### 시각화 작업(유일한 해를 갖는 경우)
```
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['axes.unicode_minus'] = False

x = np.linspace(-10, 10, 100)
y1 = x + 1
y2 = 9 - 3 * x

plt.plot(x, y1, label = 'x - y = -1')
plt.plot(x, y2, label = '3x + y = 9')

plt.xlabel('x')
plt.ylabel('y')
plt.title('연립방정식 선그래프')
plt.legend()
plt.grid()
plt.show()
```

### 파이썬 코드(무수히 많은 해를 갖는 경우)
```
import numpy as np

A = np.array([[1, 1],
              [3, 3]])
b = np.array([2, 6])

solution = np.linalg.solve(A, b)
print("해: ", solution)

- 결과
해:  [0. 2.]
```

### 시각화 작업(무수히 많은 해를 갖는 경우)
```
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['axes.unicode_minus'] = False

x = np.linspace(-10, 10, 100)

y1 = 2 - x
y2 = 2 - x

plt.plot(x, y1, label = 'x + y = 2')
plt.plot(x, y2, linestyle = '--', label = '3x + 3y = 6')

plt.xlabel('x')
plt.ylabel('y')
plt.title('연립방정식 선그래프(완전히 겹침)')
plt.legend()
plt.grid()
plt.show()
```

### 예제 1-2
```
(a) x + y = 3
    x - y = 1

(b) 2x - y = -8
    -2x + y = 4

(c) 2x - y = -2
    -2x + y = 2
```
```
[풀이]

(a) 해가 (2, 1)로 유일하다.
(b) 해가 없다.
(c) 해가 무수히 많다.
```

#### (a) 파이썬 코드
```
import numpy as np

# 계수 행렬 A
A = np.array([[1, 1],
              [1, -1]])

# 결과 벡터 b
b = np.array([3, 1])

# 해 구하기
solution = np.linalg.solve(A, b)

# 출력
print("해: ", solution)
print("x= ", solution[0])
print("y= ", solution[1])

- 결과

해:  [2. 1.]
x=  2.0
y=  1.0
```

#### (a) 시각화 작업
```
import numpy as np 
import matplotlib.pyplot as plt

# 마이너스 기호 깨짐 방지
plt.rcParams['axes.unicode_minus'] = False

# x 범위 설정
x = np.linspace(-10, 10, 100)

# 직선 식
# x + y = 3 → y = 3 - x
y1 = 3 - x

# x - y = 1 → y = x - 1
y2 = x - 1

# 선그래프 그리기
plt.plot(x, y1, label = 'x + y = 3')
plt.plot(x, y2, label = 'x - y = 1')

# 그래프 설정
plt.xlabel('x')
plt.ylabel('y')
plt.title('연립방정식 선그래프')
plt.legend()
plt.grid()

# 출력
plt.show()
```

#### (b) 파이썬 코드
```
import numpy as np

# 계수 행렬 (A)
A = np.array([[2, -1],
              [-2, 1]])

# 상수 행렬(B)
B = np.array([-8, 4])

try:
    # 방정식 풀기(Ax = B)
    solution = np.linalg.solve(A, B)
    print(solution)
    
except np.linalg.LinAlgError:
    print("결과 : 행렬식이 0입니다. 해가 없거나 무수히 많습니다.")

- 결과
결과 : 행렬식이 0입니다. 해가 없거나 무수히 많습니다.
```

#### (b) 시각화 작업
```
import matplotlib.pyplot as plt
import numpy as np

# 1. x값 범위 설정
x = np.linspace(-10, 5, 400)

# 2. 방정식을 y에 관한 식으로 변환
# 2x - y = -8 → y = 2x + 8
y1 = 2 * x + 8

# -2x + y = 4 → y = 2x + 4
y2 = 2 * x + 4

# 3. 그래프 그리기
plt.figure(figsize = (8, 6))
plt.plot(x, y1, label = '2x - y = 8', color = 'blue')
plt.plot(x, y2, label = '-2x + y = 4', color = 'red', linestyle = '--')

# 4. 그래프 꾸미기
plt.grid()
plt.legend()
plt.title('Graph of Linear Equations')
plt.xlabel('x')
plt.ylabel('y')

# 5. 화면 출력
plt.show()
```

#### (c) 파이썬 코드
```
import numpy as np

# 계수 행렬(A)
A = np.array([[2, -1], 
             [-2, 1]])

# 상수 행렬(B)
B = np.array([-2, 2])

try:
    # 방정식 풀기(Ax = B)
    solution = np.linalg.solve(A, B)
    print(solution)

except np.linalg.LinAlgError:
    print("결과: 행렬식이 0입니다. 해가 없거나 무수히 많습니다.")

- 결과
결과: 행렬식이 0입니다. 해가 없거나 무수히 많습니다.
```

#### (c) 시각화 작업
```
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(-5, 5, 400)

# y에 대해 정리 : y = 2x + 2
y1 = 2 * x + 2
y2 = 2 * x + 2

# 두 직선이 겹치므로 하나는 굵게, 하나는 점선으로 표시
plt.plot(x, y1, label = '2x - y = -2', color = 'blue', linewidth = 4, alpha = 0.3)
plt.plot(x, y2, label = '-2x + y = 2', color = 'red', linestyle = '--')

plt.grid()
plt.legend()
plt.title('Identical Lines (Infinitely Many Solutions)')
plt.show()
```

### 1. 연립일차방정식과 첨가행렬
- 계수만을 이용한 연립일차벙정식의 풀이
```
a₁₁x₁ + a₁₂x₂ + ..... = b₁
a₂₁x₁ + a₂₂x₂ + ..... = b₂
......
                      = bm
```
```
| a₁₁x₁  a₁₂x₂  ..... | b₁ |
| a₂₁x₁  a₂₂x₂  ..... | b₂ | ← 첨가행렬의 두 번째 행(2행)
| ......              |    |
|                     | bm |
  ↑ 첨가행렬의 첫 번째 열(1열)
[식 (1.6)의 첨가행렬]
```
- 첨가행렬 : 계수만을 직사각형 모양으로 배열한 것
  복잡한 연립방정식을 한눈에 보기 편하게 계수와 상수항을 합쳐서 만든 행렬
- 행(row) : 첨가행렬의 가로줄
- 열(column) : 첨가행렬의 세로줄

#### 예제 1-3
- 다음 연립일차방석은 첨가행렬로, 첨가행렬은 연립일차방정식으로 나타내라.
```
(a)
3x - z = 0
-√3x + 4y - 2z = 1
3x + 5y - 2z = 2

(b)
7x + y + z - w = 2/5
x - 6y + 2z + w = 1
x - y - 5z = -1

(c)
[-1  2 | -1]
[ 2  4 |  8]

(d)
[-1  0   4 | 1]
[ 4  2  -3 | 2]
[-3  2   1 | 1]
```
```
[풀이]

(a)
[  3    0  -1 | 0 ]
[ -√3   4  -2 | 1 ]
[  3    5  -2 | 2 ]

(b)
[ 7   1   1  -1 |  2/5 ]
[ 1  -6   2   1 |  1   ]
[ 1  -1  -5   0 | -1   ]

(c)
-x + 2y = -1
2x + 4y = 8

(d)
-x + 4z = 1
4x + 2y - 3z = 2
-3x + 2y + z = 1
```

##### ✍️작성자: 박지안
##### 🐧실습 환경: Anaconda - Jupyter
##### 🗓️ 작업일: 2026-05-31
